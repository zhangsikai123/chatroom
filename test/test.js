assert = require('assert');
// start mocha test
describe('Array', function () {
    describe('#indexOf()', function () {
        it('should return -1 when value not present', function () {
            input = [1, 2, 3];
            assert.equal(input.indexOf(4), -1)
        })
    })
});

describe('Send', function () {
    // Don't know how to write yet
});

